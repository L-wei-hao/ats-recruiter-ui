# Infrastructure

This directory contains local-development, bootstrap, and deployment assets.

- `postgres/init/` — SQL executed by the PostgreSQL container on first boot
- `caddy/` — production reverse-proxy config and HTTPS termination for the built app
