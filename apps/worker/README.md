# Worker placeholder

This directory is reserved for asynchronous worker code.

Workers will eventually handle document extraction, structured fact generation, chunking, embeddings, indexing, retries, and dead-letter handling.
