# Frontend placeholder

This directory is reserved for future frontend application structure.

The current Vite/React app still lives at the repository root while the foundation is being established. Later tasks will move or mirror the app into this location as the repo structure evolves.
